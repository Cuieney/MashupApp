

class HttpConstant{
  static const host = "http://122.51.35.22:8000";
  static const register = host + "/auth/register";
  static const login = host + "/auth/login";
  static const type = host + "/gettype";
  static const subscribe = host + "/user/subscribe";
  static const unSubscribe = host + "/user/unsubscribe";
  static const showHistory = host + "/user/showHistory";
}